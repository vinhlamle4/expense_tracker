# expense_tracker Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-03

## Active Technologies

- Dart ≥ 3.0 / Flutter ≥ 3.x + `sqflite` (local DB), `riverpod` (state management), `csv` (CSV export), `path_provider` (file paths), `share_plus` (share sheet), `fl_chart` (reports chart), `intl` (date/currency formatting) (001-expense-tracker-core)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Dart ≥ 3.0 / Flutter ≥ 3.x

## Code Style

Dart ≥ 3.0 / Flutter ≥ 3.x: Follow standard conventions

## Recent Changes

- 001-expense-tracker-core: Added Dart ≥ 3.0 / Flutter ≥ 3.x + `sqflite` (local DB), `riverpod` (state management), `csv` (CSV export), `path_provider` (file paths), `share_plus` (share sheet), `fl_chart` (reports chart), `intl` (date/currency formatting)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
